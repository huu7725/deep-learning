data tự lấy thủ công từ file collecting 

train mode điều chỉnh về cpu hay theo máy tùy theo người dùng

vì chạy trên gpu nên dữ liệu khá chậm

phần collect se tự tạo data flie trên máy nên sẽ không cần tạo file lưu trữ mói tránh bị tìm data không ra
